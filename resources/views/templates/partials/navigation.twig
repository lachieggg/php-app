<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path_for('home') }}">lachiegrant.io</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li><a href="{{ path_for('contact') }}">Contact</a></li>
                <li><a href="{{ path_for('home.thinkers') }}">Thinkers</a></li>
                <li><a href="{{ path_for('home.github') }}" target="_blank">GitHub</a></li>
                <li><a href="{{ path_for('home.resume') }}" target="_blank">CV</a></li>

                {% if auth.check %}
                    <li><a href="{{ path_for('blog') }}">Blog</a></li>
                    <li><a href="{{ path_for('home.gallery') }}">Gallery</a></li>
                    <li><a href="{{ path_for('blog.admin') }}"Blog Admin</a></li>
                    <li><a href="{{ path_for('gallery.admin') }}">Gallery Admin</a></li>
                {% endif %}

            </ul>

            <ul class="nav navbar-nav navbar-right">
                {% if auth.check %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">{{ auth.user.name }} <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <!--
                            <li><a href="{{ path_for('auth.password.change') }}">Change password</a></li>
                            -->
                            <li><a href="{{ path_for('auth.sign-out') }}">Sign out</a></li>
                        </ul>
                    </li>
                {% else %}
                    <li><a href="{{ path_for('auth.sign-up') }}"><b>Sign up</b></a></li>
                    <li><a href="{{ path_for('auth.sign-in') }}"><b>Sign in</b></a></li>
                {% endif %}
            </ul>

        </div>
    </div>
</nav>
