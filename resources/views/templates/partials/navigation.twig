<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path_for('home.home') }}">lachiegrant.io</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li><a href="{{ path_for('home.about') }}">About</a></li>
                <li><a href="{{ path_for('home.blog') }}">Blog</a></li>
                <li><a href="{{ path_for('home.readings') }}">Reading</a></li>
                <li><a href="{{ path_for('home.forum') }}">Forum</a></li>
                <li><a href="https://github.com/lachieggg" target="_blank">GitHub</a></li>

                {% if auth.check %}
                    <li><a href="{{ path_for('blog.admin') }}">Admin</a></li>
                {% endif %}

            </ul>

            <ul class="nav navbar-nav navbar-right">
                {% if auth.check %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">{{ auth.user.full_name }} <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <!--
                            <li><a href="{{ path_for('auth.password.change') }}">Change password</a></li>
                            -->
                            <li><a href="{{ path_for('auth.sign-out') }}">Sign out</a></li>
                        </ul>
                    </li>
                {% else %}
                    <li><a href="{{ path_for('auth.sign-up') }}">Sign up</a></li>
                    <li><a href="{{ path_for('auth.sign-in') }}">Sign in</a></li>
                {% endif %}
            </ul>
            <!-- search button
            <form class="form-inline my-2 my-lg-0 search-form">
              <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
              <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
            </form>
            -->
        </div>
    </div>
</nav>
