{% extends 'templates/app.twig' %}

{% block content %}
<body id='sign-in-body' class='index-body'>
	<div id='login-welcome-div'>
    <img id='welcome-img' class='my-slider-images half-img' src='images/sample.png'></img>
  </div>
  <br>
  <br>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <div class="panel panel-default">
        <div class="panel-heading">Sign in</div>
          <div class="panel-body">
            <form class='input-form' action="/sign-in" method="post" autocomplete="off">
              <div class="form-group{{ errors.email ? ' has-error' : '' }}">
                <label for="email">Email</label>
                <input type="email" name="email" id="email" placeholder="you@domain.com" class="form-control" value="{{ old_input.email }}">
                {% if errors.email %}
                  <span class="help-block">{{ errors.email | first }}</span>
                {% endif %}
              </div>
              <div class="form-group{{ errors.password ? ' has-error' : '' }}">
                <label for="password">Password</label>
                  <input type="password" name="password" id="password" class="form-control">
                    {% if errors.password %}
                      <span class="help-block">{{ errors.password | first }}</span>
                    {% endif %}
              </div>
              <button type="submit" class="btn btn-default">Sign in</button>
                {{ csrf.field | raw }}
            </form>
            <br>
            <div>
              Don't have an account? <a href="/sign-up">Sign up</a>
            </div>
          </div>
        </div>
      </div>
    </div>
    
</body>
{% endblock %}
